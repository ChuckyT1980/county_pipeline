# models/

Canonical domain models (property identity, evidence, observations,
lifecycle state) for property_intelligence_v2. Not yet populated. Distinct
from the existing root-level `property_model.py` - see the top-level
`property_intelligence_v2/README.md` for why, and for the isolation rule
before adding anything here.
