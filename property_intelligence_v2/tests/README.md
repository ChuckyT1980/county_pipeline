# tests/

property_intelligence_v2's own test suite, independent of the legacy
`tests/` directory at the repo root. Not yet populated. See the top-level
`property_intelligence_v2/README.md` for the isolation rule before adding
anything here.
