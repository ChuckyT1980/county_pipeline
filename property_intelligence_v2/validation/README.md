# validation/

Validation rules and release gates for property_intelligence_v2 - the v2
equivalent of the legacy `lead_status.py` state machine and
`release_gate.py` assertions, rebuilt on the new models. Not yet
populated. See the top-level `property_intelligence_v2/README.md` for the
isolation rule before adding anything here.
